spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: 714c842f-40fe-4009-992d-c896101b67d3
  config:
    import:
      - nacos:${spring.application.name}.properties?group=dev
      - nacos:datasource.yml?group=dev
      - nacos:mybatis.yml?group=dev
      - nacos:other.yml?group=dev

  application:
    name: gulimall-coupon

